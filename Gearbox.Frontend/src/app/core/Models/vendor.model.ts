export interface Vendor {
  id: string;
  name: string;
  phoneNumber: string;
  email: string;
  address: string;
}

export interface NewVendor {
  name: string;
  phoneNumber: string;
  email: string;
  address: string;
}
